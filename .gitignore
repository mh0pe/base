# Context and planning files (internal only — root level)
BASE-V1-SPEC.md
BASE-V2-SPEC.md
CARL-HYGIENE-SPEC.md
CARL-MCP-SPEC.md
DECISIONS-REDESIGN-SPEC.md
DIRECTORY-STRATEGY-SPEC.md

# PAUL planning directory
.paul/

# Node
node_modules/
# mcp/node_modules is created as a symlink by the SessionStart hook;
# the trailing-slash pattern above matches directories but NOT symlinks,
# so we add a slash-less entry to cover the symlink case as well.
mcp/node_modules
__pycache__/
